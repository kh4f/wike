name: CI

on: { push: { branches-ignore: 'wip/**' } }

jobs:
  ci:
    name: Checks
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Build
        run: cargo build --release

      - name: Test
        run: cargo test